% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profundizacion_radicular.R
\name{calcular_profundidad_radicular}
\alias{calcular_profundidad_radicular}
\title{Profundizacion radicular potencial (Paso 2)}
\usage{
calcular_profundidad_radicular(
  clima_estacion,
  fecha_emergencia,
  fecha_hito2,
  tb_c,
  pr_s_e,
  pr,
  profundidad_maxima_suelo = Inf,
  fecha_inicio = NULL,
  fecha_fin = NULL
)
}
\arguments{
\item{clima_estacion}{Tibble de una unica estacion (con \code{date}, \code{tx},
\code{tn}, \code{tm}), tal como devuelve \code{\link[=leer_clima_csv]{leer_clima_csv()}}.}

\item{fecha_emergencia}{Date. Fecha de emergencia (de
\code{\link[=calcular_fenologia]{calcular_fenologia()}}).}

\item{fecha_hito2}{Date. Fecha de cuaje de granos -hito2- (de
\code{\link[=calcular_fenologia]{calcular_fenologia()}}); a partir de esta fecha la profundidad se
congela.}

\item{tb_c}{Numeric. Temperatura base (°C) para el crecimiento radicular.}

\item{pr_s_e}{Numeric. Profundidad constante entre siembra y emergencia
(m).}

\item{pr}{Numeric. Tasa de profundizacion radicular (cm/°C).}

\item{profundidad_maxima_suelo}{Numeric. Profundidad maxima del suelo
(m). Por defecto \code{Inf} (sin tope).}

\item{fecha_inicio}{Date opcional. Primera fecha de la serie devuelta
(por defecto, la primera fecha disponible en \code{clima_estacion}; en la
practica deberia ser la fecha de siembra).}

\item{fecha_fin}{Date opcional. Ultima fecha de la serie devuelta (por
defecto, la ultima fecha disponible en \code{clima_estacion}).}
}
\value{
Tibble con columnas \code{date} y \code{profundidad_radical_m}.
}
\description{
Implementa la Seccion 5 del documento maestro: profundidad constante
entre siembra y emergencia, luego crecimiento a una tasa \code{pr} (cm/°C,
base \code{tb_c}) desde emergencia hasta \code{fecha_hito2} (cuaje de granos:
Z71 en trigo, R2 en maiz, R5 en soja), momento en que se congela. Esta
es la profundidad \strong{potencial}; la limitacion por sequia (PR-2 / BH-5)
pertenece al Paso 4 y no esta implementada aca.
}
\details{
A diferencia de la hoja \code{fenologia} del xlsx de referencia (que no tiene
contexto de suelo), esta funcion aplica explicitamente el tope contra la
profundidad maxima del suelo que pide el documento ("hasta la prof.
maxima del suelo o el cuaje de granos, lo que ocurra primero" -- duda
PR-1): si se provee \code{profundidad_maxima_suelo}, la profundidad calculada
nunca la supera.
}
