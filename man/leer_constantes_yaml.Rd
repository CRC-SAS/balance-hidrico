% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{leer_constantes_yaml}
\alias{leer_constantes_yaml}
\title{Leer constantes categoricas fijas (rastrojo, humedad inicial) desde YAML}
\usage{
leer_constantes_yaml(
  path = system.file("extdata", "constantes.yml", package = "balancehidrico")
)
}
\arguments{
\item{path}{Character. Ruta al YAML de constantes. Por defecto, el
archivo empaquetado \code{inst/extdata/constantes.yml}.}
}
\value{
Lista con (al menos) los elementos \code{rastrojo} y \code{humedad_inicial}.
}
\description{
A diferencia de \code{\link[=leer_parametros_yaml]{leer_parametros_yaml()}} (que siempre requiere \code{path}
explicito porque el contenido varia por escenario), esta funcion tiene un
default que apunta al archivo empaquetado, porque estas constantes NO
varian entre escenarios (ver \code{inst/extdata/constantes.yml}). El default
puede sobreescribirse (p.ej. en tests, o si CRC-SAS decide versionar
estos valores por fuera del paquete).
}
