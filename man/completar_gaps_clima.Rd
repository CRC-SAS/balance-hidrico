% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputacion_clima.R
\name{completar_gaps_clima}
\alias{completar_gaps_clima}
\title{Completar huecos puntuales en una base climatica diaria (una estacion)}
\usage{
completar_gaps_clima(clima, semilla = 1234)
}
\arguments{
\item{clima}{Tibble de una sola estacion, misma forma que devuelve
\code{\link[=leer_clima_csv]{leer_clima_csv()}} (\code{station_id}, \code{date}, \code{doy}, \code{tx}, \code{tn}, \code{tm},
\code{pp}, \code{eto}). \code{date} debe ser una secuencia diaria completa y
ordenada. La columna \code{eto} no se toca (recalcularla despues si
corresponde).}

\item{semilla}{Integer. Semilla para \code{set.seed()}. Default \code{1234}.}
}
\value{
El mismo tibble \code{clima}, con los huecos internos de
\code{tx}/\code{tn}/\code{tm}/\code{pp} completados.
}
\description{
Rellena dias sin dato en \code{tx}/\code{tn}/\code{tm}/\code{pp}, \strong{solo} cuando estan
acotados por observaciones reales de ambos lados -- las corridas de
\code{NA} que llegan hasta el primer o el ultimo dia de la serie (p.ej. el
resto de un anio en curso, todavia no observado) se dejan intactas, no
son "huecos" sino datos que todavia no existen.
}
\details{
\strong{Temperaturas}: \code{tx}, \code{tn} y \code{tm} se rellenan de forma conjunta, no
como 3 series independientes -- generarlas por separado podria dar
\code{tn > tx} (fisicamente imposible, y rompe \code{sqrt(tx-tn)} de
\code{\link[=calcular_eto_hargreaves]{calcular_eto_hargreaves()}}) o un \code{tm} inconsistente con \code{(tx+tn)/2}.
\code{tx} es la serie "primaria" (descomposicion STL, \code{stlplus}, mas un
"bridge" AR(1) sobre el residuo con la varianza de innovacion escalada
por trimestre del anio, condicionado a los valores reales inmediatos
antes/despues del hueco -- una muestra, no el promedio); \code{tn} se
deriva de \code{tx} y de un modelo aparte para el rango termico (\code{tx-tn},
en escala log para que de positivo), lo que garantiza \code{tn < tx} por
construccion; \code{tm} nunca se modela por separado, siempre se deriva al
final con \code{\link[=calcular_temperatura_media]{calcular_temperatura_media()}} (formula exacta ya usada en
el resto del paquete). Detalle completo en \code{.rellenar_temperaturas()}.

\strong{Precipitacion} (\code{pp}): modelo de dos partes tipo Richardson (1981,
WGEN) -- ocurrencia via cadena de Markov de 2 estados, monto
condicional a que llueva via una distribucion gamma.

Esta funcion siembra el generador aleatorio (\code{set.seed(semilla)}) al
principio para que el resultado sea reproducible -- efecto de lado
sobre el estado global de \code{.Random.seed}, documentado aca a proposito.
}
